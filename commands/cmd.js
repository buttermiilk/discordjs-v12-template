const Discord = require('discord.js');
const data = require('quick.db');
const settings = require('../settings.json');

exports.run = async(client, message, args) => { //eslint-no-unused-vars
  
  //add your code to run the command.
  
}

exports.help = {
  name: 'command',
  aliases: ['cmd'],
}
